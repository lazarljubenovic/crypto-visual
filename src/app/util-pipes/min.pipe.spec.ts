/* tslint:disable:no-unused-variable */

import { TestBed, async } from '@angular/core/testing';
import { MinPipe } from './min.pipe';

describe('Pipe: Min', () => {
  it('create an instance', () => {
    let pipe = new MinPipe();
    expect(pipe).toBeTruthy();
  });
});
