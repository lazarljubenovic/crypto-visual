/* tslint:disable:no-unused-variable */

import { TestBed, async } from '@angular/core/testing';
import { MaxPipe } from './max.pipe';

describe('Pipe: Max', () => {
  it('create an instance', () => {
    let pipe = new MaxPipe();
    expect(pipe).toBeTruthy();
  });
});
