/* tslint:disable:no-unused-variable */

import { TestBed, async } from '@angular/core/testing';
import { BinaryPipe } from './binary.pipe';

describe('Pipe: Binary', () => {
  it('create an instance', () => {
    let pipe = new BinaryPipe();
    expect(pipe).toBeTruthy();
  });
});
