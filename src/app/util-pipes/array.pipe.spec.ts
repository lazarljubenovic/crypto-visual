/* tslint:disable:no-unused-variable */

import { TestBed, async } from '@angular/core/testing';
import { ArrayPipe } from './array.pipe';

describe('Pipe: Array', () => {
  it('create an instance', () => {
    let pipe = new ArrayPipe();
    expect(pipe).toBeTruthy();
  });
});
