/* tslint:disable:no-unused-variable */

import { TestBed, async } from '@angular/core/testing';
import { GetPipe } from './get.pipe';

describe('Pipe: Get', () => {
  it('create an instance', () => {
    let pipe = new GetPipe();
    expect(pipe).toBeTruthy();
  });
});
