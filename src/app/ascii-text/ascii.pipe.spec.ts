/* tslint:disable:no-unused-variable */

import { TestBed, async } from '@angular/core/testing';
import { AsciiPipe } from './ascii.pipe';

describe('Pipe: Ascii', () => {
  it('create an instance', () => {
    let pipe = new AsciiPipe();
    expect(pipe).toBeTruthy();
  });
});
